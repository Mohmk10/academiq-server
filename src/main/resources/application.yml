spring:
  application:
    name: AcademiQ
  profiles:
    active: dev

server:
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
    com.academiq: DEBUG
    org.hibernate.SQL: DEBUG
